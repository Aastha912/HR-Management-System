const addButton = document.getElementById('add-button');
        const signOutButton = document.getElementById('sign-out-button');

        // Add event listeners based on your functionality requirements
        addButton.addEventListener('click', () => {
            // Handle "Add" button click (e.g., open form, trigger action)
        });

        signOutButton.addEventListener('click', () => {
            // Handle "Sign Out" button click (e.g., redirect to index page)
            // window.location.href = "index.html"; // Example redirect using form submission
        });
  